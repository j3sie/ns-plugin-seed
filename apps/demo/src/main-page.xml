<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="input-textfield" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="mytest" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        
        <Button text="simple" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>